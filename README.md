# agentic-tools

Open-source tools for agentic software development.

## Packages

- [`packages/berth`](./packages/berth) — trunk guard + agent workflow spine
  (early/experimental). Deployable into any git repo via `berth init`.
